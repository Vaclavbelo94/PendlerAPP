
import React from 'react';
import { UnifiedMobileNavbar } from '@/components/mobile/UnifiedMobileNavbar';

const ModernNavbar = () => {
  return <UnifiedMobileNavbar />;
};

export default ModernNavbar;
